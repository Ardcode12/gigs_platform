# Base URL of the authority API.
# Left as a relative path in development so Vite's proxy handles it.
# FastAPI backend used by the mobile worker/customer apps and authority portal.
VITE_API_BASE_URL=http://localhost:8001/api
